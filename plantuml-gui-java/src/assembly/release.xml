<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.0 http://maven.apache.org/xsd/assembly-2.1.0.xsd">

    <id>release</id>

    <formats>
        <format>zip</format>
    </formats>

    <!-- Everything sits at the root of the zip, so the launchers find the JAR next to them. -->
    <includeBaseDirectory>false</includeBaseDirectory>

    <files>
        <file>
            <source>${project.build.directory}/${project.build.finalName}.jar</source>
            <outputDirectory>.</outputDirectory>
        </file>
        <file>
            <source>${project.basedir}/release/launcher_java_unix.sh</source>
            <outputDirectory>.</outputDirectory>
            <fileMode>0755</fileMode>
        </file>
        <file>
            <source>${project.basedir}/release/launcher_java_windows.bat</source>
            <outputDirectory>.</outputDirectory>
        </file>
    </files>

</assembly>
